# VBR (Vision Benchmark in Rome) — lingbot_map trajectory evaluation.
workspace: /path/to/workspace/vbr

datasets:
  - vbr

methods:
  - lingbot_map

evaluation:
  traj:
    enable: true
    vis: true
  auc:
    enable: false
  depth:
    enable: false
  points:
    enable: false
