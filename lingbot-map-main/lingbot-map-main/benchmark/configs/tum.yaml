# TUM RGB-D — lingbot_map trajectory evaluation.
# Freiburg1 sequences; RGB frames associated with the nearest GT pose.
workspace: /path/to/workspace/tum

datasets:
  - tum

methods:
  - lingbot_map

evaluation:
  traj:
    enable: true
    vis: true
  auc:
    enable: false
  depth:
    enable: false
  points:
    enable: false
